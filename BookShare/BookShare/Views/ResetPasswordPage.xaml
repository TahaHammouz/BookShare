<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:BookShare.ViewModels"
             x:Class="BookShare.Views.ResetPasswordPage">
    <ContentPage.BindingContext>
        <viewModels:ResetPasswordViewModel />
    </ContentPage.BindingContext>
    <StackLayout>
        <Label Text="Reset Password" FontSize="Large" Margin="0,10" />
        <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" Margin="20,10" />
        <Button Text="Reset Password" Command="{Binding ResetPasswordCommand}" Margin="20,10" />
    </StackLayout>
</ContentPage>